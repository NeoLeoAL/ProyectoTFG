<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorAccentDark</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
        <item name="colorAccent">@color/colorPrimary</item>

        <item name="android:textColorPrimary">@color/white</item>
        <item name="backgroundColor">@color/navColorNight</item>
        <item name="rippleColor">@color/colorAccentLight</item>
        <item name="tint">@color/white</item>
    </style>

    <style name="bottomNavigationViewTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="tint">@color/colorPrimary</item>
    </style>

    <style name="fieldText" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorControlNormal">@color/transparent</item>
        <item name="colorControlActivated">@color/transparent</item>
        <item name="android:textColorHighlight">@color/transparent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ActionBarTheme" parent="ThemeOverlay.AppCompat.DayNight.ActionBar" />
    <style name="actionBarPopupTheme" parent="ThemeOverlay.AppCompat.DayNight" />
</resources>